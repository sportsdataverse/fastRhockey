# Report an API warning via cli.

Report an API warning via cli.

## Usage

``` r
.report_api_warning(w, hint = NULL, args = list())
```

## Arguments

- w:

  Captured condition.

- hint:

  Optional glue-style template evaluated against `args`.

- args:

  Named list of caller args.

## Value

invisible(NULL)
