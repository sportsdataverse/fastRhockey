# Parse plays array from API response into a data frame

Parse plays array from API response into a data frame

## Usage

``` r
.parse_plays(
  plays,
  home_abbr,
  away_abbr,
  home_id,
  away_id,
  game_id,
  season,
  season_type
)
```
