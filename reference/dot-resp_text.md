# Response body as UTF-8 text (replaces the legacy httr content text reader).

Response body as UTF-8 text (replaces the legacy httr content text
reader).

## Usage

``` r
.resp_text(resp)
```

## Arguments

- resp:

  An httr2 response.

## Value

Character scalar.
