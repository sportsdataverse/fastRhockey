% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nhl_edge_skater_speed_top_10.R
\name{nhl_edge_skater_speed_top_10}
\alias{nhl_edge_skater_speed_top_10}
\title{\strong{NHL Edge Skater Speed Top 10}}
\usage{
nhl_edge_skater_speed_top_10(positions, sort_by, season = NULL, game_type = 2)
}
\arguments{
\item{positions}{Character positions filter accepted by the Edge API
(e.g., \code{"F"} for forwards, \code{"D"} for defensemen).}

\item{sort_by}{Character sort-by key accepted by the Edge API (e.g.,
\code{"total"}).}

\item{season}{Optional 4-digit end-year (e.g., \code{2025} for the 2024-25
season), an 8-character API season (e.g., \code{"20242025"}), or \code{NULL}
(default) for the current season via the \verb{/now} endpoint.}

\item{game_type}{Integer game type. 1 = preseason, 2 = regular season
(default), 3 = playoffs.}
}
\value{
A \code{fastRhockey_data} tibble with the top-10 leaderboard, or
\code{NULL} on failure / empty response.
}
\description{
Returns the NHL Edge top-10 "fastest skaters" leaderboard
for a given \code{positions} and \code{sort_by} combination. Wraps
\verb{https://api-web.nhle.com/v1/edge/skater-speed-top-10/\{positions\}/\{sortBy\}/...}.
When \code{season} is \code{NULL} (default) the \verb{/now} endpoint is used to
fetch the current season.
}
\examples{
\donttest{
  try(nhl_edge_skater_speed_top_10(
      positions = "F",
      sort_by = "total"
  ))
}
}
\keyword{Edge}
\keyword{NHL}
\keyword{Skater}
