% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nhl_stats_team_listing.R
\name{nhl_stats_team_listing}
\alias{nhl_stats_team_listing}
\title{\strong{NHL Stats API — Team Listing}}
\usage{
nhl_stats_team_listing(team_id = NULL, lang = "en", limit = 100, start = 0)
}
\arguments{
\item{team_id}{Optional integer team id. If supplied, the by-id endpoint
is used and pagination params are ignored.}

\item{lang}{Character language code. Default \code{"en"}.}

\item{limit}{Integer maximum number of results. Default 100. Ignored
when \code{team_id} is supplied.}

\item{start}{Integer pagination start index. Default 0. Ignored when
\code{team_id} is supplied.}
}
\value{
A \code{fastRhockey_data} tibble of teams, or \code{NULL} on failure.
}
\description{
Returns the top-level team listing from the NHL Stats REST
API. When \code{team_id} is \code{NULL} (default) this hits
\verb{https://api.nhle.com/stats/rest/\{lang\}/team} and returns the full
league-wide listing; when \code{team_id} is supplied it hits
\verb{https://api.nhle.com/stats/rest/\{lang\}/team/id/\{team_id\}} and returns a
single-team payload. Distinct from \code{\link[=nhl_stats_teams]{nhl_stats_teams()}}, which hits the
per-report stats endpoints.
}
\examples{
\donttest{
  try(nhl_stats_team_listing())
  try(nhl_stats_team_listing(team_id = 10))
}
}
\keyword{Listing}
\keyword{NHL}
\keyword{Stats}
\keyword{Team}
