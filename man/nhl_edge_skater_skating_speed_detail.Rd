% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nhl_edge_skater_skating_speed_detail.R
\name{nhl_edge_skater_skating_speed_detail}
\alias{nhl_edge_skater_skating_speed_detail}
\title{\strong{NHL Edge Skater Skating Speed Detail}}
\usage{
nhl_edge_skater_skating_speed_detail(player_id, season = NULL, game_type = 2)
}
\arguments{
\item{player_id}{Integer NHL player ID (e.g., \code{8478402} for Connor
McDavid).}

\item{season}{Optional 4-digit end-year (e.g., \code{2025} for the 2024-25
season), an 8-character API season (e.g., \code{"20242025"}), or \code{NULL}
(default) for the current season via the \verb{/now} endpoint.}

\item{game_type}{Integer game type. 1 = preseason, 2 = regular season
(default), 3 = playoffs.}
}
\value{
A \code{fastRhockey_data} tibble of skating-speed metrics, or \code{NULL}
on failure / empty response.
}
\description{
Returns the NHL Edge skating-speed detail payload for a
single skater (velocity bursts, top speeds, etc.). Wraps
\verb{https://api-web.nhle.com/v1/edge/skater-skating-speed-detail/\{playerId\}/...}.
When \code{season} is \code{NULL} (default) the \verb{/now} endpoint is used to
fetch the current season.
}
\examples{
\donttest{
  try(nhl_edge_skater_skating_speed_detail(player_id = 8478402))
}
}
\keyword{Edge}
\keyword{NHL}
\keyword{Skater}
