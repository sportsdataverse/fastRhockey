% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pwhl_game_corsi.R
\name{pwhl_game_corsi}
\alias{pwhl_game_corsi}
\title{\strong{PWHL Game Corsi/Fenwick (player-level on-ice)}}
\usage{
pwhl_game_corsi(game_id)
}
\arguments{
\item{game_id}{PWHL game id (integer or character).}
}
\value{
A \code{fastRhockey_data} data frame with one row per on-ice player:\tabular{lll}{
   col_name \tab types \tab description \cr
   player_id \tab character \tab Player unique identifier. \cr
   corsi_for \tab integer \tab On-ice shot attempts for (shot + blocked_shot + goal). \cr
   corsi_against \tab integer \tab On-ice shot attempts against. \cr
   corsi_for_pct \tab numeric \tab Corsi for percentage (NA when total = 0). \cr
   fenwick_for \tab integer \tab On-ice unblocked shot attempts for (shot + goal). \cr
   fenwick_against \tab integer \tab On-ice unblocked shot attempts against. \cr
   fenwick_for_pct \tab numeric \tab Fenwick for percentage (NA when total = 0). \cr
   corsi_includes_missed \tab logical \tab Always FALSE (no missed-shot events in the HockeyTech feed). \cr
   toi_seconds \tab integer \tab Total time on ice in seconds (NA if not in shift data). \cr
   corsi_for_per60 \tab numeric \tab Corsi For per 60 minutes (NA when toi_seconds is 0 or NA). \cr
}
}
\description{
Player-level on-ice shot-attempt metrics for a PWHL game.
Corsi = shot + blocked_shot + goal; Fenwick excludes blocked shots.
Missed shots are not present in the HockeyTech feed, so both are proxies
(\code{corsi_includes_missed = FALSE}). TOI is left-joined from
\code{\link[=pwhl_game_shifts]{pwhl_game_shifts()}} so every on-ice player is retained even when shift
data is absent (resulting in \code{NA} \code{toi_seconds}).
}
\examples{
\donttest{
  try(pwhl_game_corsi(game_id = 42))
}
}
\seealso{
Other PWHL Functions: 
\code{\link{pwhl_game_shifts}()},
\code{\link{pwhl_player_toi}()}
}
\author{
Saiem Gilani
}
\concept{PWHL Functions}
