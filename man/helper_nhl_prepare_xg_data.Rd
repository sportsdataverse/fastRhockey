% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_nhl.R
\name{helper_nhl_prepare_xg_data}
\alias{helper_nhl_prepare_xg_data}
\title{Prepare PBP data for xG model predictions}
\usage{
helper_nhl_prepare_xg_data(x)
}
\arguments{
\item{x}{A play-by-play data frame produced by
\code{\link{nhl_game_pbp}} or
\code{\link{nhl_game_feed}}.}
}
\value{
A tibble with model features plus identifiers
(\code{season}, \code{game_id}, \code{event_id}, \code{strength_state}).
}
\description{
Helper function to prepare fastRhockey PBP data for expected
goals (xG) calculations.

Adapted from the hockeyR \code{helper_nhl_prepare_xg_data()} function. The feature
engineering logic (lag events, era dummies, shot metrics, one-hot
encoding) is identical to the hockeyR xG training pipeline so that the
same xgboost models can be used for inference.
}
\details{
The function:
\enumerate{
\item Filters out shootouts, penalty shots, and shift-change events.
\item Computes lag features (last event type/team, time since last, distance
from last, event zones).
\item Adds era dummies for the four training eras (2011-13, 2014-18,
2019-21, 2022+).
\item Computes tactical booleans: \code{rebound}, \code{rush}, \code{cross_ice_event}.
\item One-hot encodes \code{shot_type} and \code{last_event_type}.
\item Pads any missing model features to zero so the xgboost DMatrix
dimensions match the trained models.
}
}
\keyword{internal}
