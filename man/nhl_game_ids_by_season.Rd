% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nhl_game_ids_by_season.R
\name{nhl_game_ids_by_season}
\alias{nhl_game_ids_by_season}
\title{\strong{NHL Game IDs by Season}}
\usage{
nhl_game_ids_by_season(
  season,
  game_types = c(2L),
  team_abbr = NULL,
  sleep_rate = 0
)
}
\arguments{
\item{season}{Integer four-digit \emph{end year} of the season (e.g. \code{2025}
for the 2024-25 season). Passed through to \code{\link[=nhl_club_schedule]{nhl_club_schedule()}} which
uses the same end-year convention.}

\item{game_types}{Integer vector of NHL game types to keep.
\code{1} = preseason, \code{2} = regular season (default), \code{3} = playoffs.}

\item{team_abbr}{Optional character three-letter team abbreviation
(e.g. \code{"TOR"}). If supplied, only that team's schedule is fetched
instead of iterating all 32 NHL clubs.}

\item{sleep_rate}{Numeric seconds to \code{Sys.sleep()} between per-team
API calls when iterating all teams. Defaults to \code{0} (no delay).}
}
\value{
A \code{fastRhockey_data} / \code{data.frame} with one row per unique
game, containing \code{game_id}, \code{season}, \code{game_type}, \code{game_date},
\code{home_team}, and \code{away_team}. Returns \code{NULL} on outer failure.
}
\description{
Aggregator helper that iterates every NHL team's
season schedule (via \code{\link[=nhl_club_schedule]{nhl_club_schedule()}}) and returns a deduplicated
tidy data frame of game IDs for the requested season and game types.
Mirrors the \code{Helpers.game_ids_by_season} convenience helper from the
\code{nhl-api-py} Python client.
}
\examples{
\donttest{
  try(nhl_game_ids_by_season(season = 2025, team_abbr = "TOR"))
}
}
\keyword{Aggregator}
\keyword{Helpers}
\keyword{NHL}
