% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers_nhl.R
\name{helper_nhl_calculate_xg}
\alias{helper_nhl_calculate_xg}
\title{Calculate expected goals (xG) for play-by-play data}
\usage{
helper_nhl_calculate_xg(pbp)
}
\arguments{
\item{pbp}{A play-by-play data frame produced by
\code{\link{nhl_game_pbp}} or \code{\link{nhl_game_feed}}.
Must contain columns needed by \code{helper_nhl_prepare_xg_data()}: \code{event_type},
\code{secondary_type}, \code{period_type}, \code{period}, \code{game_seconds}, \code{x}, \code{y},
\code{x_fixed}, \code{event_team_abbr}, \code{home_abbr}, \code{away_abbr}, \code{home_skaters},
\code{away_skaters}, \code{shot_distance}, \code{shot_angle}, \code{empty_net},
\code{strength_state}, \code{event_id}, \code{season}.}
}
\value{
The original \code{pbp} data frame with an \code{xg} column appended.
\code{xg} is NA for non-shot events.
}
\description{
Adds an \code{xg} column to a play-by-play data frame using the
fastRhockey xG models (XGBoost).

The models were trained on NHL PBP data from 2010-2024 using the same
methodology as the hockeyR xG models. Three models are used:
\itemize{
\item \strong{5v5 model} — Even-strength situations
\item \strong{Special teams model} — Power play, short-handed, and other
non-5v5 situations (adds skater count features)
\item \strong{Penalty shot model} — A constant xG value for penalty shot attempts
}

Models are loaded into the package namespace on package load via
\code{.onLoad()} (see \code{zzz.R}).
}
\examples{
\dontrun{
  pbp <- nhl_game_pbp(2024020001, include_shifts = TRUE)
  pbp <- helper_nhl_calculate_xg(pbp)
  # View xG for shots
  pbp[!is.na(pbp$xg), c("event_type", "shot_distance", "shot_angle", "xg")]
}
}
